version: 0.2

phases:
  install:
    commands:
      - echo Installing Maven…
      - mvn --version
  build:
    commands:
      - echo Building JAR…
      - mvn clean package -DskipTests

artifacts:
  files:
    - target/*.jar
